listfich 
   
if(numtrozos>1)
 while(trozos[i]!=NULL){
  if(strcmp(trozos[i],"-long") long=1;
  if(strcmp(trozos[i],"-link") link=1;
  if(strcmp(trozos[i],"-acc") acc=1;
  else 
      break;
  i++;
 }
 
 
 
 
 seleccionar path 
   if(pos= -long ) 
     i++ -> no se borra
   if(pos = -link)
     i++ -> no se borra
   if(pos = -acc)
     i++ -> no se borra
   else 
   
